@model IEnumerable<Org.Ktu.Isk.P175B602.Autonuoma.ViewModels.RungtynesListVM>
@{
	ViewData["title"] = "Rungtynės";
}

<div class="main-content-header container-fluid d-flex d-row justify-content-between align-items-center p-2">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a href="@Url.Action("Create")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Sukurti naują</a>
	</span>
</div>

<div class="d-flex justify-content-center">
	<table class="entitylist table table-striped table-hover">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(m => m.FaktineData)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Seimininkai)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Sveciai)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model) {
			<tr>
				<td>
					@Html.DisplayFor(m => item.FaktineData)
				</td>
				<td>
					@Html.DisplayFor(m => item.Seimininkai)
				</td>
				<td>
					@Html.DisplayFor(m => item.Sveciai)
				</td>
				<td>
					<a href="@Url.Action("Edit", new { data = item.FaktineData, seim = item.Seimininkai, svec = item.Sveciai })" class="btn btn-primary"><i class="fa-solid fa-pen-to-square"></i> Redaguoti</a>
					<a href="@Url.Action("Delete", new { data = item.FaktineData, seim = item.Seimininkai, svec = item.Sveciai })" class="btn btn-danger"><i class="fa-solid fa-trash"></i> Šalinti</a>					
				</td>
			</tr>
			}
		</tbody>
	</table>
</div>